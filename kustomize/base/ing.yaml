apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: webapp
  labels:
    app: webapp    
spec:
  rules:
  - host: webapp.sslip.io
    http:
      paths:
      - backend:
          service:
            name: webapp
            port:
              number: 8080
        path: /
        pathType: Prefix
